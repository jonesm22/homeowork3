%cython

#problem number 2 cython

import numpy
cdef sumsq(int n):
    cdef int i = 1
    lst = [i^2 for i in range(n+1)]
    sumsq = sum(lst)
    return sumsq

import time
t = time.time()
sumsq(10000)
print time.time()-t

#initial time 5.99 ms per loop
#cython time 2.62 ms
